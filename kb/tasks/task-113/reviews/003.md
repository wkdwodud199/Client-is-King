## Review: task-113 — 2026-07-12

- **Reviewer**: Codex
- **Target**: C:/Users/wkdwo/orca/projects/Client-is-King/kb/tasks/task-113/implementation-notes.md
- **Review status**: request-changes
- **Generated by**: review=codex gpt-5.5/xhigh @codex codex-cli 0.144.1, 2026-07-12 (sandbox=workspace-write)
- **Feedback**:
  - 저장/불러오기 핵심 구현은 설계의 주요 계약을 대체로 충실히 반영했다. 정적 확인 기준으로 `GameState.SaveSchemaVersion == 1`과 `schemaVersion` 필드가 추가되어 있고, `SaveOps`는 프로브→버전→마이그레이션 훅→역직렬화→V2b 정규형→V3~V10 검증 흐름을 가진다. `SaveFileStore`도 `Application.persistentDataPath/save.json` 기본 경로, `{path}.tmp`, `File.Replace`/`File.Move`, UTF-8 no-BOM 계약을 따른다.
  - 이전 Codex 리뷰에서 발견된 V11/peek 계열 결함은 현재 코드와 테스트에 반영되어 있다. `TryPeekSave`가 V11 검증 동안 loaded state/machine을 임시 설치한 뒤 성공/실패와 무관하게 원복하고, `TryLoadGame`과 같은 `TryValidateOrderIdentity(GameState s, out ...)` 경로를 탄다. `GameManagerSaveLoadTests`와 `MainMenuSaveFlowTests`에는 recipeId/customerId/partySize/snsInflow/eventInflow 변조, 런타임 state가 세이브와 발산한 Day-N 정상 세이브, 변조 세이브 잠금 회귀가 포함되어 있어 핵심 회귀 방어가 적절하다.
  - 자동 저장 트리거 5종과 `Application.isPlaying` 가드, MainMenu 이어하기 분기, Night 저장 상태 라인, `EnsureServiceDay` 제거도 구현 노트 및 대상 코드/테스트에서 확인된다. `python -B runtime/validator/cli.py kb/tasks/task-113/design.md`는 통과했다.
  - 다만 설계의 최종 테스트 기준 두 항목이 아직 충족되지 않았다. `design.md`는 640×360 원본 캡처를 Codex가 확인하고, I16 수동 Play smoke(종료 후 이어하기 재개, 파산 잠금, 손상 파일 3종 사유 표시)를 수행하도록 요구한다. `implementation-notes.md`와 `task-113-summary.md` 모두 이 두 게이트를 대기/미수행으로 기록하고 있다. 자동 테스트가 좌표와 도메인 흐름을 상당히 커버하지만, 실제 렌더 겹침·이탈과 에디터 종료/재Play 기반 사용자 흐름은 아직 검증 근거가 없다.
  - 검증 기록도 정리 필요가 있다. 구현 노트 본문 U6 표는 428/428 EditMode, 8/8 PlayMode를 기록하지만, 이후 Codex 리뷰 수정 뒤 독립 재검증은 494/494, 9/9로 기록되어 있다. 산출물 요약은 여전히 Codex 코드 리뷰 미수행 및 428/8 기준으로 남아 있어 현재 구현 상태와 인계 문서가 서로 맞지 않는다. 이번 리뷰에서는 Unity 전체 테스트를 재실행하지 않았으므로, 최종 승인 전에는 현재 코드 기준의 compile/EditMode/PlayMode 결과를 한 세트로 다시 고정해 기록해야 한다.
  - 결론: 리뷰한 코드 경로에서 추가 코드 결함은 발견하지 못했다. 그러나 설계상 필수 acceptance gate와 최종 검증 기록이 완료되지 않아 task-113을 approved로 닫기에는 이르다.
- **Action required**:
  - 640×360 원본 캡처로 MainMenu 이어하기 블록과 Night 저장 라인의 실제 렌더 겹침·이탈·가독성을 확인하고 결과를 기록할 것.
  - design.md I16 수동 Play smoke를 수행할 것: 새 게임→Day 2 Night→Play 종료→재Play→이어하기 재개, 파산 세이브 잠금, 빈 파일/버전 99/필드 삭제 손상 파일 사유 표시.
  - 현재 코드 기준으로 compile/EditMode/PlayMode 최종 결과를 재확인하고, 구현 노트와 산출물 요약의 428/8 vs 494/9 및 “Codex 코드 리뷰 미수행” 기록 불일치를 정리할 것.
