## Review: task-113 — 2026-07-12

- **Reviewer**: Codex
- **Target**: C:/Users/wkdwo/orca/projects/Client-is-King/kb/tasks/task-113/implementation-notes.md
- **Review status**: request-changes
- **Generated by**: review=codex gpt-5.5/xhigh @codex codex-cli 0.144.1, 2026-07-12 (sandbox=workspace-write)
- **Feedback**:
  - [P1] `TryPeekSave`가 V11 주문 identity 검증을 건너뛰어, V11 손상 세이브가 MainMenu에서 정상 이어하기로 표시될 수 있습니다. 설계는 `TryPeekSave`가 `TryLoadGame`의 1~6단계와 동일하되 원복해야 한다고 명시하며, 그 6단계에는 V11 사후 검증이 포함됩니다(`design.md` F4). 하지만 현재 `GameManager.TryPeekSave`는 `TryLoadInternal`까지만 호출한 뒤 `SaveOps.BuildSummary`를 반환하고, V11은 `TryLoadGame`에서만 `TryValidateOrderIdentity`로 실행됩니다(`game/Assets/Scripts/Runtime/Managers/GameManager.cs`:503, 509, 517, 525, 540). `MainMenuController.RefreshSaveUi`는 `TryPeekSave` 성공만 보고 `continueButton.interactable = true`로 정상 분기 처리하므로(`game/Assets/Scripts/Runtime/UI/MainMenuController.cs`:95, 112, 124), 같은 주문 개수에서 `recipeId`/`customerId`/`partySize`/`snsInflow`/`eventInflow`만 변조된 파일은 요약 표시와 버튼 활성화를 통과한 뒤 클릭 시에만 로드 실패합니다. 실패 후 `RefreshSaveUi`를 다시 호출해도 같은 peek 경로 때문에 계속 정상 분기로 돌아갈 가능성이 있어, “손상 세이브는 이어하기 잠금 + 사유 표시” 계약을 깨뜨립니다.
  - [P2] V11 테스트 매트릭스가 설계 요구보다 좁습니다. 설계의 V11 기준은 정상 Service-phase 세이브에서 identity 5필드(`recipeId`, `customerId`, `partySize`, `snsInflow`, `eventInflow`) 각각의 변조를 모두 명시 실패로 고정하라고 요구합니다. 현재 `GameManagerSaveLoadTests`는 `recipeId`, `partySize`, catalog 축소, served/index 허용만 다루고(`game/Assets/Tests/EditMode/GameManagerSaveLoadTests.cs`:126, 147, 166, 188), `customerId`/`snsInflow`/`eventInflow` 변조와 V11 손상이 MainMenu/`TryPeekSave`에서 잠기는지는 검증하지 않습니다. 구현 코드가 다섯 필드를 비교하더라도, 이번 task의 핵심 회귀 방지 조건이 일부 미고정입니다.
  - 그 외 주요 구현은 설계와 대체로 일치합니다. `GameState.schemaVersion` 최상단 필드, `SaveOps`의 V2b 정규형 검사와 V3~V10 단일 검증 함수, `SaveFileStore` 원자 쓰기, `GameManager` 자동 저장 트리거 5종, `EnsureServiceDay` 제거, MainMenu/Night UI 배선은 입력 문서와 코드 기준으로 확인했습니다.
  - 검증: `python -B runtime/validator/cli.py kb/tasks/task-113/design.md`는 통과했습니다. Unity compile/EditMode/PlayMode는 이번 리뷰에서 재실행하지 않았고, 구현 노트의 `EditMode 428/428`, `PlayMode 8/8` 기록을 입력 근거로 확인했습니다. 구현 노트와 산출물 요약에 남아 있는 640×360 시각 승인 및 수동 Play smoke는 아직 별도 게이트로 미수행 상태입니다.
- **Action required**:
  - `TryPeekSave`가 `TryLoadGame`과 동일하게 V11 주문 identity 검증까지 수행하되, 성공/실패 모두 기존 `state`/`machine` 참조와 내용을 원복하도록 수정하세요.
  - V11 변조 테스트를 `customerId`, `snsInflow`, `eventInflow`까지 확장하고, V11 손상 파일이 `TryPeekSave`/MainMenu 이어하기 분기에서 잠금 + 사유 표시로 처리되는 회귀 테스트를 추가하세요.
  - 수정 후 Unity compile, EditMode, PlayMode를 다시 실행하고 결과를 구현 노트 또는 후속 기록에 남기세요.
