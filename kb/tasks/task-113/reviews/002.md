## Review: task-113 — 2026-07-12

- **Reviewer**: Codex
- **Target**: C:/Users/wkdwo/orca/projects/Client-is-King/kb/tasks/task-113/implementation-notes.md
- **Review status**: request-changes
- **Generated by**: review=codex gpt-5.5/xhigh @codex codex-cli 0.144.1, 2026-07-12 (sandbox=workspace-write)
- **Feedback**:
  - P1 — `TryPeekSave`의 V11 주문 identity 검증이 저장 파일의 `loaded` 상태가 아니라 현재 런타임 `GameManager.State`를 기준으로 주문 계획을 재생성합니다. `GameManager.TryPeekSave`는 파일을 `loaded`로 읽은 뒤 곧바로 기존 `state`/`machine`을 유지한 상태에서 `TryValidateOrderIdentity(loaded, ...)`를 호출합니다(`game/Assets/Scripts/Runtime/Managers/GameManager.cs:506`, `game/Assets/Scripts/Runtime/Managers/GameManager.cs:512`, `game/Assets/Scripts/Runtime/Managers/GameManager.cs:520`). 그런데 `TryValidateOrderIdentity` 내부의 `service.TryBuildDayPlan(...)` 호출은 매개변수 `s`가 아니라 `ServiceManager.State == GameManager.Instance.State`를 사용하고, SNS modifier/day/event effects도 그 현재 상태에서 계산합니다(`game/Assets/Scripts/Runtime/Managers/GameManager.cs:609`, `game/Assets/Scripts/Runtime/Service/ServiceManager.cs:66`, `game/Assets/Scripts/Runtime/Service/ServiceManager.cs:85`, `game/Assets/Scripts/Runtime/Service/ServiceManager.cs:98`, `game/Assets/Scripts/Runtime/Service/ServiceManager.cs:109`). 따라서 새로 실행한 MainMenu의 기본 Day 1 상태에서 Day 2 이후 Service/Settlement/Night 세이브를 peek하면, 정상 세이브도 다른 day/SNS/event 입력으로 재생성되어 손상으로 잠길 수 있습니다. 이는 설계 F4의 “TryPeekSave: 1~6 동일하되 성공이어도 항상 원복” 및 MainMenu 이어하기 계약을 깨며, design.md I16의 종료 후 재Play→이어하기 smoke에서 드러날 가능성이 큽니다.
  - 관련 테스트가 이 결함을 막지 못합니다. `GameManagerSaveLoadTests.TryPeekSave_Fails_...Order_Identity_Tampered`와 `MainMenuSaveFlowTests.RefreshSaveUi_Shows_Corrupt_Branch_When_Order_Identity_Tampered`는 저장 직후 같은 런타임 상태에서 peek하므로 현재 `GameManager.State`와 파일의 `loaded`가 우연히 같습니다. 구현 노트의 PlayMode 이어하기 검증도 persistent manager가 같은 상태를 들고 있는 흐름이라, 콜드 스타트 또는 `StartNewGame()` 후 유효한 Day 2+ 세이브를 `TryPeekSave`하는 케이스가 비어 있습니다.
  - 정적 리뷰 기준으로 저장 파이프라인(V1~V10/V2b), 원자 파일 I/O, MainMenu/Night UI 배선, V11 변조 로드 롤백의 큰 구조는 설계와 대체로 맞습니다. 다만 위 P1이 핵심 사용자 경로인 MainMenu 이어하기 표시를 직접 깨므로 승인할 수 없습니다. 이번 리뷰에서는 “리뷰 출력 파일 하나만 작성” 제한을 지키기 위해 Unity 배치 테스트나 수동 smoke는 재실행하지 않았고, 구현 노트의 검증 결과는 문서 기록으로만 확인했습니다.
- **Action required**:
  - `TryPeekSave`의 V11 검증이 반드시 `loaded` 상태를 기준으로 계획을 재생성하도록 수정하세요. 가장 작은 수정은 `TryLoadGame`과 동일하게 V11 검증 동안 `state = loaded; machine = new DayPhaseMachine(loaded);`를 임시 설치하고 `finally`에서 기존 참조로 원복하는 방식입니다. 더 구조적인 대안은 `TryBuildDayPlan`/이벤트 효과 계산을 상태 매개변수 기반 helper로 분리하는 것입니다.
  - 회귀 테스트를 추가하세요: 현재 런타임 상태를 `StartNewGame()` 또는 fresh MainMenu 상태로 둔 뒤, 유효한 Day 2 이상 Service/Settlement/Night 세이브에 대해 `TryPeekSave`가 성공하고 MainMenu 이어하기가 활성화되는 케이스가 필요합니다. 같은 조건에서 identity 변조 세이브는 계속 잠겨야 합니다.
  - 수정 후 compile/EditMode/PlayMode를 재실행하고, 미결 게이트인 640×360 시각 확인과 design.md I16 수동 Play smoke를 완료하세요.
