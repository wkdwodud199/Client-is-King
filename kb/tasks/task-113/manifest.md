# Manifest — task-113

> **Load**: 기본 로드 세트의 일부. 이 task 가 *실제로* 의존하는 것만 명시해 컨텍스트를 최소화한다.
> 여기에 없는 개념/파일은 기본적으로 열지 않는다.

- **task_id**: task-113
- **inputs**: `kb/tasks/task-113/design.md`(구현 계약 SSOT), `kb/tasks/task-113/design-review-codex.md`(Codex 교차검토·반영됨), `kb/tasks/task-110/design.md`(G2 저장 후 재개 동일 DayPlan·G4 GameState 구조·C3 회복 가능), `kb/tasks/task-111/design.md`+`implementation-notes.md`(JsonUtility 왕복 결정론 가드·EnsureServiceDay 제거 후보), `kb/tasks/task-112/design.md`+`implementation-notes.md`(activeEvents 저장·EventOps 검증 재사용, 기준선 EditMode 332/PlayMode 6), `kb/tasks/task-111/reviews/001.md`·`kb/tasks/task-112/reviews/001.md`(교훈), `kb/concepts/{project-brief,demo-scope,development-priority}.md`, 현재 `game/` 코드 기준선
- **concepts_needed**: `kb/concepts/project-brief.md`(SSOT·매니저 규약), `kb/concepts/demo-scope.md`(저장 task-113·JsonUtility·List·씬 2·신규 매니저 금지 하드캡)
- **related_files**: `game/Assets/Scripts/Runtime/DayCycle/{GameState,GameEvents}.cs`; 신규 `game/Assets/Scripts/Runtime/Save/{SaveOps,SaveSummary,SaveFileStore}.cs`; `game/Assets/Scripts/Runtime/Managers/GameManager.cs`; `game/Assets/Scripts/Runtime/Service/ServiceManager.cs`(EnsureServiceDay 제거); `game/Assets/Scripts/Runtime/Settlement/SettlementManager.cs`; `game/Assets/Scripts/Runtime/UI/{MainMenuController,NightPanelController}.cs`; `game/Assets/Scripts/Editor/SceneBuilder.cs`; `game/Assets/Tests/EditMode/*`; `game/Assets/Tests/PlayMode/*`
- **notes**: **task-113 구현 계약(design.md 구현단계·실행계획·영향파일·테스트기준)만** 구현. `GameState`→`JsonUtility` **단일 슬롯**(`persistentDataPath/save.json`) 원자 쓰기(tmp→Replace). **schemaVersion v1**, GameState 평면 유지(중첩 안 함). 순수 `SaveOps` 파이프라인: 프로브→버전→마이그레이션 훅→역직렬화→**V2b 정규형 검사(`ToJson(역직렬화)==입력` 바이트 동일)**→검증 매트릭스 V1~V11. **V11 주문 identity**는 manager 계층에서 `TryBuildDayPlan`+`ServiceOps.BuildOrders(plan,customerDefs)` 재생성 결과와 인덱스별 recipeId/customerId/partySize/snsInflow/eventInflow 비교(**SaveOps 합성 복제 금지**, served/index는 저장값 존중). List·문자열 ID **non-null 필수**. 자동 저장 5종은 **`Application.isPlaying` 가드**(EditMode 332 테스트가 파일 안 씀), 테스트는 경로 override(temporaryCachePath). **저장/peek/로드가 같은 `TryValidateState` 통과**(조용한 새게임 폴백 금지). `EnsureServiceDay` legacy 제거. `GameEvents.SaveStateChanged`. 왕복 바이트 동일(스키마 float 0). 기존 API neutral·**task-112 기준선 EditMode 332/PlayMode 6 회귀 없이 보존**. 씬 2개 하드캡·SceneBuilder 멱등. `-runTests`에 `-quit` 금지. 세이브 테스트 파일이 리포/게임 폴더에 남지 않아야. Codex 코드리뷰·640×360 시각승인·수동 smoke는 구현 후 게이트.
