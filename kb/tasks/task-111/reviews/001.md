## Review: task-111 — 2026-07-11

- **Reviewer**: Codex
- **Target**: C:/Users/wkdwo/orca/projects/Client-is-King/kb/tasks/task-111/implementation-notes.md
- **Review status**: request-changes
- **Generated by**: review=codex gpt-5.5/xhigh @codex codex-cli 0.144.1, 2026-07-11 (sandbox=workspace-write)
- **Feedback**:
  - **변경 요청**: `SNSCampaignOps.TryBuildDayModifier`의 history→DayModifier 재구성 경로가 매칭된 campaign definition의 audience row 무결성을 검증하지 않습니다. `IsDefInvalid`는 집행/미리보기 경로에서 중복 `(AgeBand, Gender)`와 잘못된 multiplier를 잡지만, DayModifier 경로는 def 조회 후 곧바로 `ProjectAffinityRows(def.AudienceAffinities)`를 호출합니다(`game/Assets/Scripts/Runtime/Social/SNSCampaignOps.cs:454-476`). 이 경우 저장 후 재개 또는 catalog 변경으로 중복 타겟 행이 들어오면 설계 C4의 “집행·plan 모두 명시적 실패” 계약과 달리 최댓값 매칭으로 조용히 plan이 만들어질 수 있습니다. 현재 테스트도 중복 audience row 실패를 `TryExecute`에만 걸고, DayModifier 재구성 테스트는 0/1/2건·미지 id·bonus 범위만 다룹니다.
  - **테스트 공백**: 설계 테스트 기준에는 `GameState`를 `JsonUtility`로 ToJson→FromJson 왕복한 뒤 `TryBuildDayPlan` 결과가 field-by-field 동일해야 한다는 항목이 있습니다. 소스/테스트 검색 기준으로 task-111 신규 경로에 대한 `JsonUtility`/`ToJson`/`FromJson` 왕복 테스트가 보이지 않습니다. SNS 효과의 핵심이 `snsCampaignHistory`에서 익일 modifier를 재구성하는 것이므로, 저장 후 재개 동일성은 자동 테스트나 명시적 후속 이관 기록이 필요합니다.
  - **대조 결과**: 핵심 수학(C1~C5), base-prefix 불변, 보너스 주문 태그/통계, UI DTO 표시, SceneBuilder catalog 주입, 비용 재시드 값은 소스상 설계와 대체로 일치합니다. 구현 노트의 Unity 검증 결과는 compile exit 0, EditMode 230/230, PlayMode 4/4로 기록되어 있습니다. 이 리뷰에서는 “리뷰 파일 하나만 작성” 규칙을 지키기 위해 Unity를 재실행하지 않았습니다. 640×360 시각 승인, 수동 Play smoke, 오너 비용 재시드 승인은 implementation-notes/summary에 적힌 대로 아직 별도 대기 게이트입니다.
- **Action required**:
  - `SNSCampaignOps.TryBuildDayModifier`에서 boost를 만들기 전에 matched campaign def의 audience rows를 검증하세요: null list 방어, multiplier 양수/finite, 중복 `(AgeBand, Gender)` 명시 실패. 해당 history→DayModifier/plan 경로의 실패 테스트를 추가하세요.
  - `GameState` `JsonUtility` 왕복 후 SNS history 기반 `TryBuildDayPlan` 동일성을 검증하는 EditMode 테스트를 추가하거나, 이 기준을 task-113으로 넘기는 설계 승인 기록을 남기세요.
  - 수정 후 compile/EditMode/PlayMode를 다시 실행하고, 시각 승인·수동 smoke·오너 비용 승인 게이트는 별도로 완료하세요.
