## Review: task-112 — 2026-07-12

- **Reviewer**: Codex
- **Target**: C:/Users/wkdwo/orca/projects/Client-is-King/kb/tasks/task-112/implementation-notes.md
- **Review status**: request-changes
- **Generated by**: review=codex gpt-5.5/xhigh @codex codex-cli 0.144.1, 2026-07-12 (sandbox=workspace-write)
- **Feedback**:
  - **[변경 요청] 런타임 이벤트 catalog 검증이 "정확히 4종"을 강제하지 않습니다.** `game/Assets/Scripts/Runtime/Events/EventOps.cs:47`의 `TryValidateCatalog`는 null/empty, 중복 ID, 중복 Kind, kind별 개별 필드만 확인합니다. 따라서 `group_customers` 같은 필수 이벤트 asset이 누락되어 catalog가 3종만 들어와도 정상 catalog로 통과하고, `TryBuildNextDayActiveEvents`/`TryBuildForecast`/`TryBuildDayEffects`가 축소된 후보군으로 조용히 계속 진행할 수 있습니다. 이는 설계의 "이벤트 정확히 4종", "catalog 손상 시 명시적 사유로 phase 진행 차단" 계약과 맞지 않습니다. 현재 `SceneBuilderTests`/PlayMode는 양씬에 4종이 주입된 정상 케이스만 확인하고, `EventOpsTests`도 중복 kind는 확인하지만 필수 kind 누락 실패 케이스는 없습니다.
  - 핵심 구현 방향은 설계와 대체로 일치합니다. `GameState.activeEvents`/`ActiveEventState` 저장 형태, `EventOps`의 FNV-1a 시드 47/문턱 450 strict `<`, Night 예고와 next-day 활성화의 동일 순수 계산 재사용, `EventDayEffects`와 `DayModifier` 분리, Market/Service/Settlement 인과 배선은 문서 계약과 부합합니다. 이벤트 asset 4종도 위생 8,000원/단체 4인 값으로 재시드되어 있습니다.
  - 테스트 범위는 넓습니다. 결정론 벡터, C4 스케줄, 예고==적용, JsonUtility 왕복, 손상 activeEvents 게이트, stale-day 할증 필터, worst-case 정산 문구 폭, PlayMode day 1→3 경로까지 포함되어 있습니다. 다만 위 catalog 필수 kind 누락 케이스가 빠져 있어 손상 데이터 방어 매트릭스가 완전하지 않습니다.
  - `design.md` G절의 면류 worked value와 구현 테스트 값이 다릅니다(설계 15,954/20,497/30,579, 구현 15,991/20,538/30,621). 구현 노트의 float32 재검산 근거는 타당해 보이며 프로덕션 코드 결함으로 보지는 않습니다. 다만 설계 SSOT와 테스트 기대값이 어긋난 상태이므로, 오너/Codex가 별도 문서 정정 또는 승인 기록을 남겨야 후속 task에서 같은 숫자를 다시 쟁점화하지 않습니다.
  - 이번 리뷰에서 Unity EditMode/PlayMode 전체 테스트는 재실행하지 않았습니다. 정적 대조와 빠른 `runtime/validator/cli.py kb/tasks/task-112/design.md` 검증은 수행했고 validator는 통과했습니다. 구현 노트의 `EditMode 330/330`, `PlayMode 6/6` 결과는 문서 근거로만 인정합니다.
  - 구현 노트가 명시한 640×360 원본 캡처 시각 승인, 수동 Play smoke, 오너 이벤트 시드 재확정 승인은 아직 미수행입니다. 이는 코드 수정 요청과는 별도 게이트지만 task-113 진행 전에는 닫혀야 합니다.
- **Action required**:
  - `EventOps.TryValidateCatalog`가 정확히 필수 4개 kind(`IngredientPriceSurge`, `HygieneInspection`, `RentIncrease`, `GroupCustomers`)를 모두 포함하고 그 외/누락이 없음을 검증하도록 수정하세요.
  - `EventOpsTests`에 필수 kind 하나가 누락된 catalog가 명시적으로 실패하는 테스트를 추가하고, 가능하면 `GameManager` phase gate가 해당 손상 catalog를 차단하는 회귀도 보강하세요.
  - 위 변경 후 Unity compile/EditMode/PlayMode 결과를 implementation-notes 또는 후속 구현 노트에 갱신하세요.
