## Review: task-115 — 2026-07-12

- **Reviewer**: Codex
- **Target**: C:/Users/wkdwo/orca/projects/Client-is-King/kb/tasks/task-115/implementation-notes.md
- **Review status**: approved
- **Generated by**: review=codex gpt-5.5/xhigh @codex codex-cli 0.144.1, 2026-07-12 (sandbox=workspace-write)
- **Feedback**:
  - 구현은 task-115의 핵심 제약을 충족한다. `GameState`는 새 직렬화 필드 없이 `SaveSchemaVersion = 1`을 유지하고, 엔딩은 `EndingOps.GetStatus`에서 `isBankrupt` 우선·`daysCompleted >= 7` 클리어로 파생된다. `GameManager`도 `CanAdvancePhase`/`AdvancePhase` 양쪽에서 클리어 상태를 파산 게이트와 대칭으로 차단한다.
  - 설계 원안의 운영비 28,000원은 구현 중 실측 오류가 확인되어 15,000원으로 재확정되었고, 구현 노트가 그 근거와 오너 승인 사실을 기록한다. 코드상 `SettlementOps.DailyOperatingCost`와 `EventOps.BaseDailyOperatingCost`는 15,000원으로 동기화되어 있으며, `BalanceEndingGuardTests`가 상수 핀·전 장르 7일 실루프 클리어·무위 플레이 Day 3 이내 파산·클리어 저장 왕복을 고정한다. 이는 원안과 다른 수치지만, 설계의 밴드 절차가 깨진 이유를 재실측으로 설명하고 새 승인값을 테스트에 반영했으므로 수용 가능하다.
  - 엔딩 UI와 MainMenu 분기도 설계 의도에 맞다. `EndingOverlayController`는 Canvas 탑재 폴링 방식으로 비활성 오버레이를 표시하고, `SceneBuilder.BuildEndingOverlay`는 640×360 모달·최상단 sibling·초기 비활성을 구성한다. `MainMenuController.RefreshSaveUi`는 파산 우선 후 클리어 세이브를 잠그고 새 게임을 유도한다.
  - Windows 빌드 도구는 `BuildTool.ScenePaths()`로 [MainMenu, Shop] 2씬 순서를 검증하고, `BuildWindows()`가 StandaloneWindows64 빌드 실패를 예외로 전파한다. 리뷰 중 `game/Build/Windows/ClientIsKing.exe` 존재와 `.gitignore:38`의 `game/[Bb]uild*/` 적용을 확인했다.
  - 검증 기록은 충분하다. 구현 노트는 EditMode 486/486, PlayMode 9/9, BuildWindows exit 0을 기록한다. 리뷰 중 별도로 `runtime/validator/cli.py kb/tasks/task-115/design.md`, `runtime/generate-status.py --check`, `runtime/validator/cli.py --check-done task-115`가 모두 통과함을 확인했다. Unity 전체 테스트는 리뷰 단계에서 재실행하지 않았다.
  - 오너 플레이테스트, 640×360 시각 승인, 빌드 exe 더블클릭 스모크는 구현 노트와 산출물 요약에 미결 게이트로 명확히 남아 있다. 이 리뷰의 approved는 코드/자동 검증/문서 일관성 기준 승인이지, 해당 수동 게이트 통과나 M3 최종 선언을 대체하지 않는다.
- **Action required**:
  - 없음
