## Review: task-110 — 2026-07-11

- **Reviewer**: Codex
- **Target**: C:/Users/wkdwo/orca/projects/Client-is-King/kb/tasks/task-110/implementation-notes.md
- **Review status**: request-changes
- **Generated by**: review=codex gpt-5.5/xhigh @codex codex-cli 0.144.1, 2026-07-11 (sandbox=workspace-write)
- **Feedback**:
  - **변경 요청 — 640×360 시각 승인 게이트 미완료.** 설계는 `kb/tasks/task-110/design.md:692`에서 640×360 원본 캡처를 Codex가 검토해 modal/detail/forecast/HUD badge 겹침, canvas 이탈, 좌표·폰트·focus 순서 일치를 승인하라고 요구한다. 구현 노트와 summary는 이 항목을 미수행/대기 상태로 명시한다(`implementation-notes.md:150-158`, `task-110-summary.md:49`). 코드상 `SceneBuilder`가 `Panel_GenreSelection`, `GenreBadge`, `GenreEffectText` 등을 생성하고 테스트가 오브젝트 존재·좌표 일부를 검증하지만, 실제 렌더 캡처 증빙은 없다. 이 게이트 없이는 UI 가독성과 겹침 리스크를 승인할 수 없다.
  - **변경 요청 — 수동 Play smoke 미완료.** 설계는 `design.md:567` 및 `design.md:702`에서 첫 의미 있는 선택이 60초 안에 나오는지, 네 장르의 예상 주문 수·주 고객층·1인 가격 차이가 화면과 실제 transaction에서 일치하는지 수동 smoke로 확인하라고 요구한다. 구현 노트와 summary는 자동 PlayMode 도메인 검증은 했지만 실제 화면 조작 기반 수동 smoke는 수행하지 않았다고 적고 있다(`implementation-notes.md:157-158`, `task-110-summary.md:50`). `GenrePersistencePlayModeTests`는 persistent instance와 UI 없는 `AdvancePhase` 원자성을 확인하므로 좋은 회귀 테스트지만, 이 수동 인수 기준을 대체하지는 못한다.
  - **구현 적합성 확인.** 정적 코드 리뷰 기준으로 핵심 구현은 설계와 대체로 일치한다. `GameState.selectedGenreId`는 문자열 상태로 추가되었고, `GenreSelectionOps`는 Day 1 Market/첫 구매 전 선택 게이트, `RoundHalfUp`, FNV-1a known vector, specialist/generalist plan 생성을 구현했다. `GameManager`의 Market→Service/Service→Settlement 게이트, `EconomyOps`/`ServiceOps`의 장르 배수 overload, `MarketPanelController`/`PhaseHudController`/`ServicePanelController`/`SettlementPanelController`의 UI 표시 경로도 설계 방향과 맞다.
  - **테스트 커버리지 확인.** 구현 노트는 compile exit 0, EditMode 152/152, PlayMode 2/2를 기록한다(`implementation-notes.md:130-132`). 코드상 `GenreSelectionOpsTests`, `GenreBalanceTests`, scene/UI GenreFlow tests, PlayMode persistence tests가 설계의 결정론·밸런스·도메인 게이트·씬 배선을 넓게 커버한다. 다만 이번 리뷰에서는 “리뷰 파일 하나만 작성” 제약을 지키기 위해 Unity 테스트를 재실행하지 않았고, repo 안에서 별도 test result XML/log 증빙은 확인하지 못했다.
  - **기록 정합성 주의.** 현재 git 상태와 summary는 task-110 구현 커밋 `9265da5`가 완료되었다고 보이지만, `implementation-notes.md:16`과 `implementation-notes.md:164`에는 아직 “커밋 대기/커밋하지 않음” 문구가 남아 있다. 기능 동작 리스크는 아니지만 handoff 문서로는 혼동을 만든다.
- **Action required**:
  - 640×360 원본 캡처 기반 시각 검토를 수행하고, 장르 modal/detail/forecast/HUD badge의 겹침·이탈·좌표·폰트·focus 순서 승인 결과를 기록한다.
  - 실제 화면 조작 기반 수동 Play smoke를 수행해 60초 내 첫 선택, 선택 전 구매/Service 차단, 네 장르별 forecast와 실제 transaction/정산 원인 문구 일치를 확인한다.
  - `implementation-notes.md`의 stale “커밋 대기/커밋하지 않음” 문구를 현재 커밋 상태와 맞춘다.
